name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  gates:
    uses: ./.github/workflows/ci.yml

  build:
    runs-on: ubuntu-latest
    needs: gates
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check version consistency
        run: |
          PY_VERSION="$(python3 -c "import tomllib; print(tomllib.load(open('python/pyproject.toml','rb'))['project']['version'])")"
          NPM_VERSION="$(node -p "require('./ts/package.json').version")"
          if [ "$PY_VERSION" != "$NPM_VERSION" ]; then
            echo "manifest version mismatch: pyproject=$PY_VERSION package.json=$NPM_VERSION"
            exit 1
          fi
          case "$GITHUB_REF" in
            refs/tags/v*)
              TAG_VERSION="${GITHUB_REF#refs/tags/v}"
              if [ "$TAG_VERSION" != "$PY_VERSION" ]; then
                echo "tag v$TAG_VERSION does not match manifest version $PY_VERSION"
                exit 1
              fi
              ;;
          esac
          echo "version consistency ok: $PY_VERSION"
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.29"
      - name: Build Python distributions
        working-directory: python
        run: |
          rm -rf dist
          uv build --no-create-gitignore
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: ts/package-lock.json
      - name: Build npm tarball
        working-directory: ts
        run: |
          npm ci
          rm -rf dist
          npm run build
          npm pack
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-dist
          path: python/dist/*
          if-no-files-found: error
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-tarball
          path: ts/verifiably-nodes-*.tgz
          if-no-files-found: error

  verify-artifacts:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: python-dist
          path: dist-python
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: npm-tarball
          path: dist-npm
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.29"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
      - run: python3 .github/scripts/verify_python_artifacts.py dist-python
      - run: python3 .github/scripts/verify_npm_tarball.py dist-npm/*.tgz
      - run: .github/scripts/smoke_install_python.sh dist-python
      - run: .github/scripts/smoke_install_npm.sh dist-npm/*.tgz
      - name: PyPI publish dry-run
        run: uv publish --dry-run --trusted-publishing never dist-python/*.whl dist-python/*.tar.gz
      - name: npm publish dry-run
        run: npm publish --dry-run "./$(ls dist-npm/*.tgz)"

  publish-npm:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [gates, build, verify-artifacts]
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: npm-tarball
          path: dist-npm
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org
      - name: Require npm >= 11.5.1 (trusted publishing floor)
        run: |
          node -e "
            const [maj, min, pat] = require('child_process').execSync('npm --version').toString().trim().split('.').map(Number);
            const ok = maj > 11 || (maj === 11 && (min > 5 || (min === 5 && pat >= 1)));
            if (!ok) { console.error('npm too old for trusted publishing'); process.exit(1); }
          "
      - run: npm publish "./$(ls dist-npm/*.tgz)"

  publish-pypi:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [gates, build, verify-artifacts]
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: python-dist
          path: dist
      - uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: "0.11.29"
      - name: PyPI pre-upload check
        run: python3 .github/scripts/pypi_upload_check.py pre --project verifiably-nodes --version "${GITHUB_REF#refs/tags/v}" --dist dist
      - name: Generate PyPI attestations
        run: uvx --from pypi-attestations==0.0.29 python -m pypi_attestations sign dist/*.whl dist/*.tar.gz
      - name: Validate signed PyPI upload set
        run: uv publish --dry-run --trusted-publishing never
      - name: Publish to PyPI
        run: uv publish --trusted-publishing always --check-url https://pypi.org/simple/
      - name: PyPI post-upload check
        run: python3 .github/scripts/pypi_upload_check.py post --project verifiably-nodes --version "${GITHUB_REF#refs/tags/v}" --dist dist
